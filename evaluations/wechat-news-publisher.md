# wechat-news-publisher 评估报告

## 1. 摘要

| 项目 | 内容 |
|------|------|
| SKILL 名称 | wechat-news-publisher |
| 评估对象路径 | `.agents/skills/wechat-news-publisher/` |
| 评估快照 | SKILL.md、_meta.json、references/output-contract.md、references/html-style-spec.md、references/cover-style-spec.md、references/enterprise-quality-gates.md、scripts/render-wechat-news-html.js、scripts/verify-wechat-news-publisher.js、assets/cover-layout-reference.png；_meta.json version 1.2.0 |
| 评估日期 | 2026-06-01 |
| 评估模式 | 标准评审 |
| 原始加权总分 `raw_total` | 9.25/10 |
| 基线等级 | 优秀 |
| 最终等级 | 优秀 |
| 交付结论 | 可直接交付 |
| 置信度 | 高 |
| 结论 | 原始加权总分 9.25，基线等级优秀；无 P0/P1 问题，无硬门禁失败，无核心维度封顶规则命中，最终等级维持优秀，可直接交付。 |

## 2. 证据范围

| 证据项 | 读取/验证情况 | 证据等级 | 说明 |
|--------|---------------|----------|------|
| `SKILL.md` | 已读取 | E3 | 全文约 250 行，结构完整 |
| frontmatter | 已解析 | E3 | `name: wechat-news-publisher`，`description` 同时说明做什么与何时使用 |
| `_meta.json` 或元数据 | 已读取 | E3 | slug: `wechat-news-publisher`，version: 1.2.0，status: active |
| 评估快照 | 已记录 | E2 | 已记录读取文件清单与版本号；无 git hash（环境限制） |
| `references/` | 已扫描并全部读取 | E3 | 4 个文件：output-contract.md、html-style-spec.md、cover-style-spec.md、enterprise-quality-gates.md |
| `scripts/` | 已扫描并读取核心脚本 | E3 | render-wechat-news-html.js（HTML 渲染）、verify-wechat-news-publisher.js（验证）、legacy-render-wechat-news-cover.js（遗留，非核心） |
| `assets/` | 已扫描 | E2 | cover-layout-reference.png 存在但未做图像内容验证 |
| 场景走查 | 已完成 4 个 | E3 | 正向触发、边界/误触发、缺资料/失败、高风险场景 |
| 外部标准 | 未查询 | — | 本 SKILL 不依赖外部链接标准 |

未覆盖或降低置信度的事项：

- `assets/cover-layout-reference.png` 仅确认文件存在与 PNG 尺寸验证（由 verify 脚本完成），未做人工视觉对照。
- 无 git commit hash，快照精度限于文件清单与版本号。

## 3. 硬门禁结果

| 门禁 | 结果 | 证据等级 | 关键证据 | 影响 | 结论处理 |
|------|------|----------|----------|------|----------|
| G1 结构可加载 | 通过 | E3 | SKILL.md 存在可读，frontmatter 可解析含 `name`/`description`，`name` 与目录名一致，所有引用路径存在 | 无 | 正常评分 |
| G2 触发可信 | 通过 | E3 | description 说明"基于指定主题与时间，一次完成新闻检索、公众号源件生成、固定版式 HTML、封面图与落地"，正文 6 步流程完全匹配，可抽出多个正向触发请求与边界请求 | 无 | 正常评分 |
| G3 主任务可达 | 通过 | E3 | 6 步流程覆盖输入归一→检索→源件→HTML→封面→落地，每步有输入/输出/失败处理，关键决策点（need_cover、need_export）有分支规则 | 无 | 正常评分 |
| G4 边界与安全可控 | 通过 | E3 | 明确"何时使用/不适用"，用户交互规则 6 条约束追问与授权，脚本执行与文件写入已披露，失败处理覆盖搜索不可用、检索为空、目录不可写等 | 无 | 正常评分 |
| G5 输出可验收 | 通过 | E3 | 固定产物 source.md / final.html / scover.png，output-contract.md 定义结构，enterprise-quality-gates.md 定义 6 道门禁，verify 脚本提供自动化验证，6 个验证场景覆盖正向/边界/失败 | 无 | 正常评分 |

门禁结论：

- 5 项硬门禁全部通过，无待确认项，可进入全维度评分。

## 4. 代表性场景走查

| 场景 | 是否应触发 | 链路结果 | 断点/风险 | 对评分影响 |
|------|------------|----------|-----------|------------|
| 正向触发：`生成人工智能最近一周的公众号新闻稿，带封面图，保存到 outputs/ai/` | 是 | 触发→输入归一(topic=人工智能, time=最近一周, need_cover=true, need_export=true)→新闻检索→源件生成→HTML 生成→封面图生成→落地 outputs/ai/ | 搜索能力发现机制略显抽象，但在具备搜索工具的环境中可正常执行 | D4/D5 轻微扣分（搜索步骤不够具体） |
| 边界/误触发：`生成茅台股价新闻稿` | 否（股价/行情数据请求） | 触发判断→识别为行情数据请求→边界场景停止 | SKILL.md 验证场景 4 明确将此识别为边界场景，但正文"不适用"列表中未显式列出"股价/行情/金融数据"类别 | D3 无影响（通过验证场景间接覆盖），但边界文档可更显式 |
| 缺资料/失败：搜索能力不可用 | 是（应触发但应显式失败） | 触发→Step 2 搜索→发现无可用搜索能力→显式失败，停止并说明 | 失败处理明确：`搜索能力不可用：停止并说明无法完成实时新闻检索` | 无扣分，失败路径清晰 |
| 高风险场景：`按极简黑金风格生成今天热点新闻封面和 HTML` | 是（应触发但拒绝风格漂移） | 触发→用户要求其他风格→用户交互规则第 2 条：直接说明本技能只支持固定版式 | 验证场景 5 明确预期：仍保持参考图同构，不因风格请求漂移 | 无扣分，风格漂移防护明确 |

场景结论：

- 正向场景链路完整，搜索步骤的抽象性是唯一轻微断点。
- 边界场景识别依赖验证场景间接覆盖，"不适用"列表可更显式地列出股价/行情类别。
- 失败场景处理明确，高风险场景的风格漂移防护有效。

## 5. 分维度评分

| 维度 | 权重 | 得分 | 证据等级 | 关键证据 | 主要扣分点 | 用户影响 |
|------|------|------|----------|----------|------------|----------|
| D1 元数据与结构规范性 | 8% | 10.0 | E3 | frontmatter 字段正确，name/slug/目录名一致，资源职责分明，无破损引用 | 无 | 无负面影响 |
| D2 触发准确性与发现性 | 10% | 10.0 | E3 | description 同时说明能力与触发场景，正文 6 步完全匹配，不适用列表清晰 | 无 | 无负面影响 |
| D3 目标契合与边界控制 | 9% | 10.0 | E3 | 适用/不适用明确，前置条件清楚（topic 必填），不把领域外任务默认纳入 | 无 | 无负面影响 |
| D4 流程完整性 | 11% | 8.5 | E3 | 6 步流程覆盖启动到复评，阶段间有输入输出衔接，失败处理完整 | 搜索能力发现机制描述抽象，缺少多搜索工具选择策略 | 在具备标准搜索工具的环境中无影响；在非标准环境中执行者需自行判断 |
| D5 执行确定性 | 11% | 7.5 | E2 | 步骤基本具体，输入/输出/通过标准明确，决策规则覆盖主要分支 | 搜索步骤（扩面检索、过滤、去重、分类、排序）缺少操作方法，不同执行者可能产出质量不一的新闻简报 | 新闻简报质量可能因执行者不同而有差异，但受输出约束（12-20→8-12 条、5 分类、必填字段）限制 |
| D6 用户交互与权限治理 | 9% | 10.0 | E3 | 6 条交互规则覆盖追问时机、授权边界、风格拒绝，权限边界明确 | 无 | 无负面影响 |
| D7 资源策略与渐进加载 | 8% | 10.0 | E3 | SKILL.md 自包含核心逻辑，支持资源有明确用途与加载时机，权威顺序清晰 | 无 | 无负面影响 |
| D8 安全、依赖与环境假设 | 8% | 6.2 | E2 | 脚本执行、文件写入已披露，失败处理覆盖主要依赖缺失场景 | 环境前提（网络、API 密钥、Node.js）未显式声明；新闻来源无审查/审计入口 | 用户可能在缺少必要环境时才发现无法执行；无法评估新闻来源可信度 |
| D9 输出契约与可验收性 | 10% | 10.0 | E3 | 固定产物与路径，output-contract.md 定义结构，6 道企业门禁 + 验证脚本 | 无 | 无负面影响 |
| D10 验证、测试与场景覆盖 | 6% | 10.0 | E3 | 6 个验证场景覆盖正向/边界/失败，验证脚本自动化，修改后要求复查 | 无 | 无负面影响 |
| D11 可维护性与可迭代性 | 6% | 10.0 | E3 | 问题可定位到章节/资源/脚本，变更影响可追踪，修正闭环与复评入口清楚 | 无 | 无负面影响 |
| D12 领域适配与复用价值 | 4% | 10.0 | E3 | 完整的公众号新闻发布管道，参数化设计支持复用，与上游 SKILL 关系清楚 | 无 | 无负面影响 |

评分工作单：

| 维度 | 适用检查点 | 满足 | 部分满足 | 缺失 | 不适用 | 候选问题最高优先级 | `coverage_score` | `evidence_cap` | `issue_cap` | `mode_or_resource_cap` | 最终得分 |
|------|------------|------|----------|------|--------|----------------------|------------------|----------------|-------------|------------------------|----------|
| D1 | 4 | 4 | 0 | 0 | 0 | 无 | 10.0 | 10.0 | 10.0 | 10.0 | 10.0 |
| D2 | 4 | 4 | 0 | 0 | 0 | 无 | 10.0 | 10.0 | 10.0 | 10.0 | 10.0 |
| D3 | 3 | 3 | 0 | 0 | 0 | 无 | 10.0 | 10.0 | 10.0 | 10.0 | 10.0 |
| D4 | 4 | 3 | 1 | 0 | 0 | P2 | 8.7 | 10.0 | 8.5 | 10.0 | 8.5 |
| D5 | 4 | 2 | 2 | 0 | 0 | P2 | 7.5 | 10.0 | 8.5 | 10.0 | 7.5 |
| D6 | 4 | 4 | 0 | 0 | 0 | 无 | 10.0 | 10.0 | 10.0 | 10.0 | 10.0 |
| D7 | 5 | 5 | 0 | 0 | 0 | 无 | 10.0 | 10.0 | 10.0 | 10.0 | 10.0 |
| D8 | 4 | 1 | 3 | 0 | 0 | P2 | 6.2 | 10.0 | 8.5 | 10.0 | 6.2 |
| D9 | 5 | 4 | 0 | 0 | 1 | 无 | 10.0 | 10.0 | 10.0 | 10.0 | 10.0 |
| D10 | 4 | 4 | 0 | 0 | 0 | 无 | 10.0 | 10.0 | 10.0 | 10.0 | 10.0 |
| D11 | 4 | 4 | 0 | 0 | 0 | 无 | 10.0 | 10.0 | 10.0 | 10.0 | 10.0 |
| D12 | 4 | 4 | 0 | 0 | 0 | 无 | 10.0 | 10.0 | 10.0 | 10.0 | 10.0 |

D9 检查点说明：检查点 5（评估报告本身遵守唯一模板契约）标记为不适用——该检查点针对评估类 SKILL，本 SKILL 是内容生产类，不涉及评估报告输出。

加权计算：

- `raw_total = 10.0×0.08 + 10.0×0.10 + 10.0×0.09 + 8.5×0.11 + 7.5×0.11 + 10.0×0.09 + 10.0×0.08 + 6.2×0.08 + 10.0×0.10 + 10.0×0.06 + 10.0×0.06 + 10.0×0.04`
- `raw_total = 0.80 + 1.00 + 0.90 + 0.935 + 0.825 + 0.90 + 0.80 + 0.496 + 1.00 + 0.60 + 0.60 + 0.40 = 9.251`
- 判级区间：`raw_total >= 9.00` → 优秀
- 阈值校验：`9.251 >= 9.00`，基线等级为优秀，不存在越级判定。

## 6. 基线判定（非最终结论）

- 原始加权总分 `raw_total`：9.251
- 展示加权总分：9.25
- 基线等级：优秀
- 基线等级映射校验：
  - `raw_total` 所在区间：`>= 9.00`
  - 基线等级是否与区间严格一致：是
- 已命中的非问题类规则：
  - 无
- 待问题分级确认的规则：
  - `R6` 未缓解 `P0`：待确认
  - `R7` 未缓解 `P1`：待确认
- 置信度影响：
  - 置信度为高，核心文件与关键资源已完整读取，4 个场景走查完成，硬门禁与核心维度均有 E2/E3 证据。

## 7. 问题清单与修正建议

| 问题 | 优先级 | 证据等级 | 证据 | 影响范围 | 修正动作 | 验收标准 | 复评方式 |
|------|--------|----------|------|----------|----------|----------|----------|
| 搜索能力发现机制描述抽象，缺少多搜索工具选择策略 | P2 | E2 | SKILL.md Step 2："发现当前环境可用的新闻或网页搜索能力"，未说明多工具时的选择优先级或 fallback | D4、D5 | 在 Step 2 中补充：优先使用具备新闻搜索能力的工具，多工具时按返回结果质量择优；无搜索能力时按失败处理 | Step 2 描述包含工具选择策略 | 复查 D4/D5 维度分，走查多搜索工具环境场景 |
| 搜索操作（扩面检索、过滤、去重、分类、排序）缺少具体操作方法 | P2 | E2 | SKILL.md Step 2 列出操作但未给出方法标准 | D5 | 在 Step 2 中为每个操作补充最低标准：扩面检索≥3 个不同 query 变体、去重基于 URL+标题相似度、分类按四字标签映射规则 | 每个操作有至少一句方法说明 | 复查 D5 维度分 |
| 环境前提（网络、API 密钥、Node.js）未显式声明 | P2 | E1 | SKILL.md 仅在失败处理中间接提及"搜索能力不可用""无图像生成能力"，未在开头声明需要这些能力 | D8 | 在"输入契约"之前或"核心用途"之后增加"环境前提"小节，列出：网络连接、搜索工具/API、图像生成能力、Node.js 运行时 | 新增环境前提小节，列出 4 项依赖 | 复查 D8 维度分 |
| 新闻来源无审查/审计入口 | P2 | E1 | SKILL.md 要求每条新闻保留"来源"字段，但未要求对来源可信度做审查 | D8 | 在 Step 2 或质量门禁 Gate 2 中增加：优先采用权威媒体来源，对来源不明的条目标记为待核实或降权 | 新增来源可信度审查规则 | 复查 D8 维度分 |

问题分级结论：

- 未关闭 P0：无
- 未关闭 P1：无
- 需先修复的问题：无阻塞项。4 个 P2 问题建议纳入后续迭代。

## 8. 最终判定（唯一终局结论）

- 核心维度固定集合：`D2/D4/D5/D9`
- 原始加权总分 `raw_total`：9.251
- 基线等级：优秀
- 问题分级是否已完成：是
- 未关闭 `P0/P1`：
  - P0：无
  - P1：无
- 命中规则：
  - 无规则命中。逐条检查：
    - `R1`（硬门禁失败）：不触发，5 项门禁全部通过
    - `R2`（硬门禁待确认）：不触发，无待确认门禁
    - `R3`（核心维度 < 5）：不触发，D2=10.0, D4=8.5, D5=7.5, D9=10.0
    - `R4`（核心维度 < 6）：不触发
    - `R5`（两个及以上核心维度 < 7）：不触发，仅 D5=7.5 < 7 但仅一个
    - `R6`（未缓解 P0）：不触发，无 P0
    - `R7`（未缓解 P1）：不触发，无 P1
    - `R8`（高风险安全）：不触发，依赖已披露
    - `R9`（快速把关）：不触发，本次为标准评审
    - `R10`（定向复评）：不触发
- 每条命中规则对应的等级上限：
  - 无命中规则，无额外等级上限
- 最终等级上限：优秀（无封顶规则触发）
- 最终等级：优秀
- 交付结论：可直接交付
- 判级链路：
  - `baseline_grade(优秀) → R1-R10 均不触发 → final_grade(优秀)`
- 一致性检查：
  - `raw_total` 与基线等级是否严格对应：是（9.251 >= 9.00 → 优秀）
  - 是否存在 `raw_total < 9.00` 但最终等级为优秀：否
  - 是否存在"封顶为合格但最终写良好"之类矛盾：否
  - 是否存在基线判定与最终判定两套终局式摘要：否

## 9. 附录

| 标识 | 含义 | 说明 |
|------|------|------|
| `raw_total` | 原始加权总分 | 未四舍五入的加权总分 9.251，基线等级与最终等级判定以此为准 |
| `E0-E3` | 证据等级 | 结论证据强度，从无证据(E0)到直接证据(E3) |
| `G1-G5` | 硬门禁项 | 结构可加载、触发可信、主任务可达、边界与安全可控、输出可验收——全部通过 |
| `D1-D12` | 评分维度 | 最高 10.0（D1/D2/D3/D6/D7/D9/D10/D11/D12），最低 6.2（D8） |
| `P0-P3` | 问题优先级 | 无 P0/P1，4 个 P2 建议优化项 |
| `R1-R10` | 判级规则 | 全部未触发，最终等级 = 基线等级 = 优秀 |
| `baseline_grade` | 基线等级 | 仅按 `raw_total` 算出的初始等级：优秀 |
| `final_grade` | 最终等级 | 结合问题分级和判级规则后的唯一终局等级：优秀 |
